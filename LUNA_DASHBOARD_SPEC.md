# LUNAレンタルサロン 店舗運営ダッシュボード 仕様書

作成日:2026-09-16
作成者:藤田聡之(チャット内プロトタイプをもとにClaude Codeでの本開発用に整理)

---

## 1. 目的

LUNAレンタルサロン(Bellezza・Forest・Asteriaの3店舗)の売上・稼働状況を可視化し、日々の運営判断(価格改定、営業時間の見直し、集客チャネルへの投資配分など)に活用するダッシュボードアプリを構築する。

チャット内でReactプロトタイプを作成済み(`luna_dashboard.jsx`)。本仕様書はそれをClaude Codeで本格的なWebアプリとして再構築するための要件をまとめたもの。

## 2. 対象ユーザー

- 藤田聡之本人(店舗運営者・意思決定者)、単独利用を想定。将来的に店舗スタッフへの共有もありうる。

## 3. データソースとデータモデル

### 3.1 データ由来

- 元データ:予約管理システムからエクスポートしたExcel(`シンBellezza利用時間202608.xlsx`)の「データ」シート
- 2023年8月〜2026年9月(取得時点)の全予約ログ、1予約=1行

### 3.2 レコード単位のデータモデル

プロトタイプで使用しているCSV列定義:

| 列名 | 型 | 説明 |
|---|---|---|
| date | string (YYYY-MM-DD) | 利用日 |
| store | string | 店舗名(Bellezza / Forest / Asteria) |
| user | string | 利用者名 |
| revenue | number(円) | この予約の売上(算定ルールは4.2参照) |
| hoursUsed | number(時間) | 実際の利用時間(算定ルールは4.3参照) |
| hour | number(0-23) | 利用開始時刻(時) |
| weekday | string | 曜日(月〜日) |
| channel | string | 集客チャネル(自社サイト / Instabase / スペースマーケット / その他) |

元Excelの主な列: 項番, サロン, 月, 日付, 開始時間, 終了時間, 時間, 氏名, 開始日付, 金額, 決裁日付, ステータス, 予約期間, 定額フラグ, 延長フラグ, 変更, 曜日。上記CSVはこれを変換・整形したもの。

### 3.3 データ量の目安

- 実績行数:約4,800件(2023-08-05〜2026-09-16、本日以降の未確定予約は除外済み)
- 店舗別内訳:Bellezza 2,907件 / Forest 1,787件 / Asteria 139件

## 4. ビジネスルール・前提条件(要レビュー)

以下はチャット上で本人と合意した前提。実装時にコードコメント等で明示すること。

### 4.1 稼働可能時間

- 1店舗あたり **14時間/日** を稼働可能時間として稼働率を算出する。
- 店舗の営業開始日は、データ上のその店舗の初回利用日から自動推定している(固定値ではない)。
  - Bellezza: 2023-08-05〜
  - Forest: 2024-10-31〜
  - Asteria: 2026-04-10〜
  - ⚠️ 実際の契約上の開業日と異なる場合は要修正。

### 4.2 売上の算定ルール

- ステータスが「利用済み」「キャンセル(返金あり)」の金額を売上に含める(キャンセル料収入も売上として計上)。
- 「キャンセル(顧客)」は売上に含めない(データ上も金額0)。
- 「定期クーポン」(定期利用/サブスクリプション収入)は店舗に紐付かないため、現行ダッシュボードの店舗別集計からは**除外**している。別収益ラインとして扱うか要検討。

### 4.3 稼働時間(hoursUsed)の算定ルール

- ステータスが「利用済み」の予約のみ、実際の利用時間としてカウントする。
- キャンセル(返金の有無問わず)は稼働時間としてはカウントしない(売上には計上されても部屋は使われていないため)。

### 4.4 集計対象期間

- 集計基準日(今日)以降の未来予約は、実績が確定していないため集計から除外している(現在の基準日:2026-09-16)。
- 本開発では「基準日」をサーバー側の現在日時から動的に算出する必要がある。

### 4.5 年度の定義

- 「年度別比較」は暦年(1月〜12月)を採用。

### 4.6 集客チャネル(導線)の判定ルール

元Excelでは「氏名」セルの文字色でチャネルを表現している(隠れた凡例。ヘッダー等には明記なし)。本人へのヒアリングで判明した対応関係:

| 文字色(RGB) | チャネル |
|---|---|
| 青(FF0070C0) | Instabase(Instagram経由) |
| 緑(FF00B050) | スペースマーケット |
| 黒 / 色指定なし(デフォルト) | 自社サイト |
| 紫(FF7030A0)など上記以外 | その他(不明。実績1件のみ) |
| 赤(FFFF0000) | 定期クーポン(サブスク)行の表示色 — チャネルではなく店舗非紐付けの別カテゴリなので除外 |

⚠️ 本開発でこの判定を再現する場合、Excelを直接読み込むならセルの文字色から同様に判定できるが、通常の予約システムAPI連携に移行した場合は「チャネル」を明示的なデータ項目として予約システム側または入力フォームで持たせる設計に変えることを推奨する(色による暗黙的な管理は属人化・誤判定のリスクが高い)。

## 5. 画面構成・機能一覧(プロトタイプ実装済み)

1. **ヘッダー**:年セレクタ(表示年度の切り替え)、CSVテンプレートダウンロード、CSVアップロード
2. **全体サマリー**:選択年の平均稼働率、平均利用時間/件、利用件数、利用者数
3. **店舗別サマリーカード**(Bellezza / Forest / Asteria):売上、稼働率、平均利用時間、総利用時間、利用件数
4. **店舗別売上推移**:選択年の月別売上を店舗別の折れ線グラフで表示
5. **稼働率比較**:選択年の店舗別稼働率を横棒グラフで比較
6. **利用時間帯分析**:開始時刻(0〜23時)別の利用時間を店舗別積み上げ棒グラフで表示
7. **曜日別稼働率**:月〜日の曜日別に店舗ごとの稼働率を比較
8. **導線別売上分析**:チャネル別の売上額(横棒グラフ)+ 店舗×チャネルの売上構成比(表)
9. **年度比較(YoY)**:選択年 vs 前年の月別売上推移グラフ + 店舗別「直近月の前年同月比」テーブル
10. **利用者別売上トップ10**:売上上位10名のランキング表(利用回数・総利用時間・平均利用時間・総売上)
11. **直近の利用実績テーブル**:選択年の直近12件(日付・店舗・利用者・導線・売上・利用時間)

## 6. 現行プロトタイプの制約(チャット内版)

- データはReactコンポーネント内にCSV文字列として埋め込み。ブラウザで動作確認するのみで、**永続化(保存)は行われない**。
- CSVアップロードはその場でブラウザのメモリ上に読み込むだけで、リロードすると初期データ(埋め込み済みの実績)に戻る。
- 複数ユーザーでの同時利用や、店舗スタッフによる入力・更新は想定していない単純な閲覧用プロトタイプ。

## 7. 本開発(Claude Code)への要件

### 7.1 データ永続化

- 現行のCSV埋め込み方式から、**データベース**(例:SQLite → 将来的にPostgreSQL)へ移行する。
- テーブル設計案:

```
transactions(
  id, date, store, user_name, revenue, hours_used,
  start_hour, weekday, channel, status, created_at
)
```

- 新しい予約データを追加・更新できるCSVインポート機能(現行のアップロードUIを踏襲)、または将来的に予約システムのAPI連携を検討。

### 7.2 技術スタック(現行踏襲を推奨)

- フロントエンド:React + Recharts(現行プロトタイプのグラフ構成をそのまま活用可能)
- バックエンド:Node.js(Express)または Python(FastAPI)+ 上記DB
- 認証:個人利用が基本のため簡易なもので可(将来的な共有を見据えるならシンプルなログイン機構を用意)

### 7.3 集計ロジックの移植

- 本仕様書 4章のビジネスルール(稼働時間14h/日、売上算定、店舗開業日、年度定義、チャネル判定)をサーバー側の集計ロジックとして実装する。
- 特に「基準日(本日)」「店舗開業日」はハードコードせず、設定値として管理できるようにする。

### 7.4 デプロイ

- 個人利用のため、まずはローカル環境またはシンプルなクラウド(Vercel / Render 等)へのデプロイで十分。

## 8. 未確定事項・要確認(引き継ぎ後に本人へ確認すべき点)

- [ ] Forest(2024-10-31〜)・Asteria(2026-04-10〜)の開業日は自動推定値。実際の契約開業日と異なれば修正が必要。
- [ ] チャネル判定で「その他」に分類された紫色セル1件の内容確認。
- [ ] 「定期利用(サブスク)」収入(店舗非紐付け)をダッシュボードにどう組み込むか(現状は除外)。
- [ ] 稼働可能時間14時間/日が実際の営業時間・予約可能枠と一致しているか。
- [ ] チャネル(導線)の今後の入力方法(Excelの色管理から明示的なデータ項目への移行)。

## 9. 参考ファイル

- `luna_dashboard.jsx` — チャット内で作成したReactプロトタイプ(本仕様書の画面構成・集計ロジックの実装例)
- `luna_利用実績_2023-2026.csv` — 元Excelから本仕様のルールに沿って整形済みの実績データ
